# Copy to .env (server-side only — never ship secrets in the client bundle)
JWT_SECRET=change-me-in-production
DATABASE_URL=postgresql://user:pass@localhost:5432/medikiosk
OCR_PROVIDER_KEY=change-me
AI_SUMMARY_KEY=change-me
SESSION_COOKIE_SECURE=true
CORS_ORIGIN=https://your-hospital-domain
